# .gitignore not ignoring folder

git rm --cached -r public/
git add .gitignore

https://stackoverflow.com/questions/39014896/gitignore-not-ignoring-folder


## Revert specific file

git checkout <commit_hash> <file_path>
